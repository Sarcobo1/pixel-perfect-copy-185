// Replaced — auth is now handled by Replit Auth headers (X-Replit-User-Id etc.)
// This file is kept as a stub so any lingering imports don't break the build.
export const attachSupabaseAuth = null;
